description = "코딩 테스트 추출 [문제 URL]"
prompt = """
{{args}}
browsermcp를 이용해서 코딩문제를 다음 조건에 맞춰 저장해줘.
`problems` 폴더 안에 문제 제목 이름(.challenge-title element)으로 폴더를 생성한 뒤에 그 안에 test.md 파일을 만들어서 문제를 정리해줘.
문제 내용(.guide-section element)에 이미지 태그가 있다면 이미지도 같은 폴더에 다운로드하고 test.md에서 해당 이미지를 로컬 경로 기준으로 참조하게 해줘.
"""